import React from "react";
import MainContainer from "../MainContainer";

const Loading = () => {
  return (
    <MainContainer
      titleText={'Loading'}
    >
      <p>Generating page</p>
    </MainContainer>
  );
};

export default Loading;
